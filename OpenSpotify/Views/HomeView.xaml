<UserControl x:Class="OpenSpotify.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converter="clr-namespace:OpenSpotify.Services.Util"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converter:ImageCoverter x:Key="ImageCoverter"/>
        <converter: x:Key="VisibilityConverter"/>
    </UserControl.Resources>
    <Grid  Margin="48,0,0,0">

        <TextBlock  Text="{Binding ApplicationModel.Settings.StatusText}" FontFamily="CalibriLight" FontSize="18" Foreground="Green" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ListBox ItemsSource="{Binding ApplicationModel.DownloadCollection}" Visibility="{Binding ApplicationModel.Settings.IsReady, Converter={StaticResource VisibilityConverter}, FallbackValue=Hidden}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="0.2*"/>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" Source="{Binding CoverImage, Converter={StaticResource ImageCoverter}}" Height="80" Width="80"/>

                        <Grid Grid.Column="1">  
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Margin="5,0,0,0" Text="{Binding SongName}"/>
                            <TextBlock Grid.Row="1" Margin="5,0,0,0" Text="{Binding SongName}"/>
                            <TextBlock Grid.Row="2" Margin="5,0,0,0" Text="{Binding SongName}"/>
                            <controls:MetroProgressBar Margin="5,0,0,0" Grid.Row="3"/>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
